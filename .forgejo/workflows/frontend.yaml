on: [push]

enable-email-notifications: true

jobs:
  build-frontend:
    runs-on: dagger
    steps:
      - uses: actions/checkout@v6

      - name: Build frontend
        run: dagger call build-frontend

  deploy-frontend:
    runs-on: dagger

    needs:
      - build-frontend

    if: forge.ref_name == 'forgejo-workflows' # TODO: switch to latest branch

    steps:
      - uses: actions/checkout@v6

      - name: Deploy frontend
        env:
          TOKEN: ${{ forge.token }}
        run: |
          set -x

          dagger call deploy-frontend --token env://TOKEN

